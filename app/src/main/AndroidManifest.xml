<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.trainingsapp.chrisals.dyel20">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ExerciseCreatorActivity" />
        <activity android:name=".ExerciseDetailActivity" />
        <activity android:name=".WorkoutCreatorActivity" />
        <activity android:name=".ExerciseSelectorActivity" />
        <activity
            android:name=".NavigationDrawerViewActivity"
            android:label="@string/title_activity_navigation_drawer_view"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".WorkoutDetailViewActivity"></activity>
    </application>

</manifest>